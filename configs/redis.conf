# ============================================================================
# REDIS CONFIGURATION - FIXED & SECURED
# Сервер: 10.0.1.210
# Назначение: Кэш, сессии, очереди Bitrix24
# ============================================================================

# СЕТЬ
bind 0.0.0.0
port 6379
protected-mode yes
tcp-backlog 511
timeout 0
tcp-keepalive 300

# ОБЩИЕ НАСТРОЙКИ
daemonize yes
supervised systemd
pidfile /var/run/redis/redis-server.pid
loglevel notice
logfile /var/log/redis/redis-server.log
databases 16

# СОХРАНЕНИЕ ДАННЫХ (SNAPSHOTING)
save 900 1
save 300 10
save 60 10000
stop-writes-on-bgsave-error yes
rdbcompression yes
rdbchecksum yes
dbfilename dump.rdb
dir /var/lib/redis

# РЕПЛИКАЦИЯ (если нужно)
# replicaof <masterip> <masterport>
# masterauth <master-password>
replica-serve-stale-data yes
replica-read-only yes
repl-diskless-sync no
repl-ping-replica-period 10
repl-timeout 60
repl-disable-tcp-nodelay no
repl-backlog-size 1mb
repl-backlog-ttl 3600
replica-priority 100

# БЕЗОПАСНОСТЬ (ИСПРАВЛЕНО)
# Установите надежный пароль вместо примера ниже
requirepass "B1tr1x_R3d1s_S3cur3_P@ssw0rd_2026!"

# Переименование опасных команд
rename-command FLUSHDB ""
rename-command FLUSHALL ""
rename-command DEBUG ""
rename-command CONFIG ""

# ОГРАНИЧЕНИЯ ПАМЯТИ (ИСПРАВЛЕНО)
maxmemory 8gb
maxmemory-policy allkeys-lru

# LRU/LFU настройки
maxmemory-samples 10

# APPEND ONLY MODE (AOF)
appendonly no
appendfilename "appendonly.aof"
appendfsync everysec
no-appendfsync-on-rewrite no
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb
aof-load-truncated yes
aof-use-rdb-preamble yes

# LUA СКРИПТЫ
lua-time-limit 5000

# CLUSTER (отключен)
cluster-enabled no

# МЕДЛЕННЫЕ ЗАПРОСЫ (LOGGING)
slowlog-log-slower-than 10000
slowlog-max-len 128

# LATENCY MONITOR
latency-monitor-threshold 100

# СОБЫТИЯ (NOTIFY)
notify-keyspace-events ""

# РАСШИРЕННЫЕ НАСТРОЙКИ
hash-max-ziplist-entries 512
hash-max-ziplist-value 64
list-max-ziplist-size -2
list-compress-depth 0
set-max-intset-entries 512
zset-max-ziplist-entries 128
zset-max-ziplist-value 64
hll-sparse-max-bytes 3000
stream-node-max-bytes 4096
stream-node-max-entries 100
activerehashing yes
client-output-buffer-limit normal 0 0 0
client-output-buffer-limit replica 256mb 64mb 60
client-output-buffer-limit pubsub 32mb 8mb 60
hz 10
dynamic-hz yes
aof-rewrite-incremental-fsync yes
rdb-save-incremental-fsync yes
